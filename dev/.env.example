# GitHub Actions Runner - Environment Configuration
# Copy this file to .env and configure with your values
#
# Note for ARM64 macOS users:
# - Docker will automatically build for linux/amd64 platform
# - This may be slower due to emulation but ensures production compatibility
# - Ensure Docker Desktop has "Use cross-platform features" enabled

# Required: Your GitHub repository or organization URL
# Examples:
#   - Repository: https://github.com/username/repository
#   - Organization: https://github.com/organization
GITHUB_URL=https://github.com/your-org/your-repo

# Required: Runner registration token from GitHub
# Get this from: GitHub Settings > Actions > Runners > New self-hosted runner
# Note: Tokens expire after 1 hour, generate fresh token for each use
RUNNER_TOKEN=your_runner_token_here

# Optional: Custom name for your runner (defaults to container hostname)
RUNNER_NAME=dev-runner

# Optional: Labels for targeting specific jobs (comma-separated)
RUNNER_LABELS=self-hosted,Linux,X64,container,dev

# Optional: Features to install (space-separated)
# Available: nodejs python docker
RUNNER_FEATURES=nodejs python docker