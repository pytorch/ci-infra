# Karpenter NodePool fleet: r7a
# AMD EPYC (Genoa) — memory-optimized, AVX-512, ~8 GiB/core ratio
fleet:
  name: r7a
  arch: amd64
  gpu: false
  exclude_regions:
    - us-west-1
  release:
    - type: r7a.48xlarge
      weight: 100
      node_disk_size: 4800
