# Karpenter NodePool definition: g4dn.12xlarge (GPU compute, 4x T4)
# Used by: pytorch-gpu-t4
nodepool:
  name: g4dn-12xlarge
  instance_type: g4dn.12xlarge
  arch: amd64
  disk_size: 200
  max_pods_per_node: 4
  gpu: true
