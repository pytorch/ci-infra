// Karpenter (zap JSON logs)
stage.match {
    selector = "{namespace=\"karpenter\"}"
    stage.json {
        expressions = {
            level = "level",
        }
    }
    stage.labels {
        values = {
            level = "",
        }
    }
}
