[project]
name = "node-compactor"
version = "0.1.0"
description = "Karpenter node compactor — taints underutilized nodes to consolidate workloads"
requires-python = ">=3.12"
dependencies = [
    "lightkube==0.15.5",
    "prometheus_client>=0.21.0",
]
