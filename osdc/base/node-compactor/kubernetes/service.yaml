apiVersion: v1
kind: Service
metadata:
  name: node-compactor
  namespace: kube-system
  labels:
    app.kubernetes.io/name: node-compactor
    app.kubernetes.io/component: controller
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 8080
      targetPort: metrics
      protocol: TCP
  selector:
    app.kubernetes.io/name: node-compactor
